# Python cache and test artifacts
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/

# Node.js dependencies and build outputs
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# Environment files
.env
.env.*
!.env.example
!**/.env.example

# Logs and local runtime files
*.log
*.pid
*dump*.txt

# Mutable runtime config — edited via /api/sim/settings/runtime, never
# committed.  Defaults live in api_server/config/runtime_defaults.json.
api_server/config/runtime.json

# Editor and OS files
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local planning artifacts
task_plan.md
findings.md
progress.md
git_status_out.txt
REFACTOR_PLAN*
plans/

# IDE / AI tool config
.windsurf/
.roo/
.claude/
CLAUDE.md

# Internal AI session tracking artifacts
docs/ux/
roo-code-settings-optimized.json

# Local test artifact folders
normalized_artifacts_runtime_test/
normalized_artifacts_test/

# Tests — run locally only, not pushed to remote
tests/

# Docs — internal design/plan docs stay local
# Only FEATURE_SPEC.md is pushed; docs/ux/ contains AI session artifacts
docs/**
!docs/FEATURE_SPEC.md

# Dataset files — large binary data, distributed via Drive/Kaggle instead
# See datasets/DATASET_MANIFEST.md and datasets/DOWNLOAD_GUIDE.md for download instructions
datasets/

# Normalized ETL artifacts — committed to repo for tester convenience
# To rebuild locally: python etl_pipeline/normalize.py
# normalized_artifacts/
# === AI tooling artifacts (managed by .windsurf-template) ===
.claude/
.code-review-graph/
.codex_appdata/
.codex
.cursor/
.gitnexus/
.roo/
.rooignore
.roomodes
.worktrees/
roo-code-settings-optimized.json
AGENTS.md
CLAUDE.md
progress.md
task_plan.md

